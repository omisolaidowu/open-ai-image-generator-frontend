const handlesubmit = (prompt, n, imageSize)=>{

    prompt.current.value = "Enter a descriptive image prompt"
    n.current.value = "How many images"
    imageSize.current.value = "Enter image size...must be square"

    //Task: Remove selected time from the database on submit

}


export default handlesubmit
